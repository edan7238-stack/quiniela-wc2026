# Configuracion de la app web (Streamlit Community Cloud y local).
[browser]
gatherUsageStats = false

[theme]
primaryColor = "#2563eb"
base = "light"

[server]
# Local: false abre el navegador solo. En Streamlit Cloud este valor lo gestiona la plataforma.
headless = false
