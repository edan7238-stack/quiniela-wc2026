# Plantilla de secretos. Copia este archivo a "secrets.toml" (mismo directorio) y pon tu clave.
# El secrets.toml real está en .gitignore y NO se sube al repositorio.
#
# En Streamlit Cloud no uses este archivo: ve a Manage app -> Settings -> Secrets
# y pega ahí la línea siguiente con tu contraseña.
app_password = "cambia-esta-clave"
