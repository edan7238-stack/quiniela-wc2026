# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Secretos de Streamlit (contraseña de la app) — NUNCA subir al repo
.streamlit/secrets.toml

# IDE / SO / herramientas locales
.idea/
.vscode/
.claude/
.DS_Store
Thumbs.db

# NOTA (despliegue): data/ y models/ SI se incluyen en el repo a proposito,
# porque la app en Streamlit Cloud los necesita y no puede regenerarlos
# (no tiene el archive.zip de Kaggle ni corre el entrenamiento en la nube).
